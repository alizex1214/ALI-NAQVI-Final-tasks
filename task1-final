# Task 1: Fundamentals of Python (Interactive Version)

# Step 1: Print Welcome Message
print("Welcome to Crop Yield Analyzer")

# Step 2: Take Farmer Details as Input
farmer_name = input("Enter farmer's name: ")              # String
farm_size = int(input("Enter farm size (in acres): "))    # Integer
main_crop = input("Enter main crop: ")                    # String
productivity_rate = float(input("Enter productivity rate (tons/acre): "))  # Float
is_organic = input("Is the farm organic? (yes/no): ").lower() == "yes"     # Boolean

# Display the farmer's details
print("\n--- Farmer Details ---")
print("Farmer Name:", farmer_name)
print("Farm Size (acres):", farm_size)
print("Main Crop:", main_crop)
print("Productivity Rate (tons/acre):", productivity_rate)
print("Organic Farm:", is_organic)

# Step 3: If-Else Condition
if farm_size > 10:
    print("\nFarm Category: Large farm")
else:
    print("\nFarm Category: Small farm")

# Step 4: Using a Loop to Generate Crop List
sample_crops = ["Wheat", "Rice", "Corn"]

print("\n--- Sample Crop List ---")
for crop in sample_crops:
    print(crop)

# Final Output
print("\nData recorded successfully for farmer:", farmer_name)
